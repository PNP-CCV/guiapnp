name: Editoracao Review Gate

on:
  push:
    branches:
      - editoracao
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  open-pr-to-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Review gate
        run: |
          echo "Branch editoracao atualizada. O PR para deploy deve ser criado manualmente."
